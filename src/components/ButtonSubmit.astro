---
const { textButton } = Astro.props;
---

<button
  type="submit"
  class="px-4 py-2 rounded-sm bg-primary dark:bg-primaryDark hover:bg-secondary text-dark dark:text-light"
  >{textButton}</button
>
